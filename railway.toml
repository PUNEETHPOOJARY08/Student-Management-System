[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && java -jar target/student-management-system-1.0.0.jar"
healthcheckPath = "/api/students"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

